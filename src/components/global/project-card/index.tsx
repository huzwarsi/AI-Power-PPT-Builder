"use client";
import React from 'react'

type Props = {
    projectId: string
    title: string
    createdAt: string
    src: string
    isDelete?: boolean
    slideData: JsonValue


}

const ProjectCard = (props: Props) => {
    return (
        <div> </div>

    )
}

export default ProjectCard